flask
gTTS
SpeechRecognition
pyaudio
gpiozero
lgpio

# System dependencies required:
# sudo apt-get install portaudio19-dev python3-lgpio
# Note: venv should be created with --system-site-packages for lgpio
